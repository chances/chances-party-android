<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:background="@drawable/list_item">

    <ImageView
        android:id="@+id/playlist.icon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="@dimen/list_icon_margin"
        android:layout_marginBottom="@dimen/list_icon_margin"
        android:contentDescription="@string/playlist_icon" />

    <TextView
        android:id="@+id/playlist.name"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:layout_margin="@dimen/text_margin"
        android:gravity="center_vertical|start"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textAlignment="gravity"
        android:ellipsize="end"
        android:lines="1" />

    <TextView
        android:id="@+id/playlist.trackCount"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_margin="@dimen/text_margin"
        android:gravity="center_vertical|end"
        android:textColor="@color/colorAccentLightControl"
        android:textAppearance="?attr/textAppearanceListItemSmall"
        android:textAlignment="gravity"
        android:lines="1" />
</LinearLayout>
